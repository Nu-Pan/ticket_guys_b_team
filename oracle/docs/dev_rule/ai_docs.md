# AI によるドキュメントメンテナンスについての規則

- いわゆる `<tgbt-root>/docs` のような、AI による編集が可能なドキュメントは存在してはいけない
- 正本は `<tgbt-root>/oracle` であり、それを正本とした実装が存在するとすれば、仕様などのドキュメント類は中間物であり、この中間物はメンテナンスを要する
- メンテナンスを正しく行うためには専用の仕組みを必要とし、それは Codex CLI を普通に使うだけでは実現できないと判断し、 `<tgbt-root>/docs` の存在禁止という形で割り切ることにした
