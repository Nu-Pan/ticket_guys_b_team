
# `tgbt eval oracle` サブコマンド仕様

## 前提・概要

- 前提として `<repo-root>/oracle` は AI 編集禁止であり、人間が内容に対して 100% 責任を負うことになっている
- が、それはそれとして、人間が責任を全うするために AI による補助機能は必要である
- そこで `tgbt eval oracle` サブコマンドでは、人間が書いた `<repo-root>/oracle` の内容を評価し、人間にフィードバックを返す
