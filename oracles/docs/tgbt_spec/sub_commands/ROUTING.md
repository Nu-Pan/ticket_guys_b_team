# `eval_oracles.md`

- `tgbt eval oracles` のサブコマンド仕様。
- AI 編集禁止の `<repo-root>/oracles` を人間が責任を持って保守するために、AI が内容を評価して人間へフィードバックする位置づけを記述する。

# `plan.md`

- `tgbt plan` のサブコマンド仕様。
- plan の役割、AI による plan 生成・更新、正本 json と閲覧用 markdown の扱いを記述する。
- 機械検査、AI レビュー、修正、停止判定、レポートからなる plan 編集ワークフローを記述する。

# `run.md`

- `tgbt run` のサブコマンド仕様。
- 現時点では未実装のままでよいことを記述する。
