# local
from util.error import tgbt_error


def tgbt_run_impl() -> None:
    """
    `tgbt run` の実装。
    """
    raise tgbt_error(
        "tgbt run は未実装です",
        "将来の作業として tgbt run を実装する必要があります",
    )
