interface:
  display_name: "Apply Oracle to Code"
  short_description: "Sync tgbt code with oracle changes"
  default_prompt: "Use $apply-oracle-to-implements to sync tgbt implementation with recent oracle changes."
