interface:
  display_name: "Apply Oracle to Implements"
  short_description: "Reflect oracle changes into tgbt implementation"
  default_prompt: "Use $apply-oracle-to-implements to reflect recent oracle changes into the tgbt implementation."
