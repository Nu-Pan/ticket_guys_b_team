interface:
  display_name: "Create Repo Local Skill"
  short_description: "tgbt repo-local skill の作成・更新・検証を行う"
  default_prompt: "Use $create-repo-local-skill to maintain and validate repo-local skills in the tgbt repository."
